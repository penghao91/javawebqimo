<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.questionnaire.dao.QuestionnaireDao">

    <resultMap id="QuestionnaireResultMap" type="com.questionnaire.model.Questionnaire">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="createdBy" column="created_by"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="isStarred" column="is_starred"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="deletedTime" column="deleted_time"/>
        <result property="folderId" column="folder_id"/>
        <association property="creator" column="created_by" javaType="com.questionnaire.model.User" select="com.questionnaire.dao.UserDao.findById"/>
    </resultMap>

    <select id="findById" parameterType="int" resultMap="QuestionnaireResultMap">
        SELECT * FROM questionnaire WHERE id = #{id}
    </select>

    <select id="findByUserId" parameterType="int" resultMap="QuestionnaireResultMap">
        SELECT * FROM questionnaire WHERE created_by = #{userId} AND is_deleted = 0 ORDER BY update_time DESC
    </select>

    <select id="findAll" resultMap="QuestionnaireResultMap">
        SELECT * FROM questionnaire WHERE is_deleted = 0 ORDER BY update_time DESC
    </select>

    <insert id="insert" parameterType="com.questionnaire.model.Questionnaire" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO questionnaire (title, description, created_by, status, is_starred, is_deleted, folder_id) 
        VALUES (#{title}, #{description}, #{createdBy}, #{status}, 
                COALESCE(#{isStarred}, 0), 
                COALESCE(#{isDeleted}, 0), 
                #{folderId})
    </insert>

    <update id="update" parameterType="com.questionnaire.model.Questionnaire">
        UPDATE questionnaire SET title = #{title}, description = #{description}, status = #{status}, 
        is_starred = #{isStarred}, is_deleted = #{isDeleted}, folder_id = #{folderId} WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="int">
        DELETE FROM questionnaire WHERE id = #{id}
    </delete>

    <update id="updateStatus">
        UPDATE questionnaire SET status = #{status} WHERE id = #{id}
    </update>

    <update id="updateStarred">
        UPDATE questionnaire SET is_starred = #{isStarred} WHERE id = #{id}
    </update>

    <update id="updateDeleted">
        UPDATE questionnaire SET is_deleted = #{isDeleted}, deleted_time = #{deletedTime} WHERE id = #{id}
    </update>

    <select id="findExpiredDeleted" parameterType="java.util.Date" resultMap="QuestionnaireResultMap">
        SELECT * FROM questionnaire WHERE is_deleted = 1 AND deleted_time &lt;= #{expireTime}
    </select>

    <update id="updateFolder">
        UPDATE questionnaire SET folder_id = #{folderId} WHERE id = #{id}
    </update>

    <select id="findActiveByUserId" parameterType="int" resultMap="QuestionnaireResultMap">
        SELECT * FROM questionnaire WHERE created_by = #{userId} AND is_deleted = 0 ORDER BY update_time DESC
    </select>

    <select id="findStarredByUserId" parameterType="int" resultMap="QuestionnaireResultMap">
        SELECT * FROM questionnaire WHERE created_by = #{userId} AND is_starred = 1 AND is_deleted = 0 ORDER BY update_time DESC
    </select>

    <select id="findAllStarred" resultMap="QuestionnaireResultMap">
        SELECT * FROM questionnaire WHERE is_starred = 1 AND is_deleted = 0 ORDER BY update_time DESC
    </select>

    <select id="findDeletedByUserId" parameterType="int" resultMap="QuestionnaireResultMap">
        SELECT * FROM questionnaire WHERE created_by = #{userId} AND is_deleted = 1 ORDER BY update_time DESC
    </select>

    <select id="findAllDeleted" resultMap="QuestionnaireResultMap">
        SELECT * FROM questionnaire WHERE is_deleted = 1 ORDER BY update_time DESC
    </select>

    <select id="findByFolderId" parameterType="int" resultMap="QuestionnaireResultMap">
        SELECT * FROM questionnaire WHERE folder_id = #{folderId} AND is_deleted = 0 ORDER BY update_time DESC
    </select>

</mapper>